{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; if [[ \"$f\" == *.ts || \"$f\" == *.tsx ]]; then bunx oxfmt --write \"$f\"; fi; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
